 /*!
 *
 * Depends:
 *		jquery.ui.v.js
 */
( function($) {
	$.extend($.ui.spine.prototype, {
        foo: function(jObj, options) {
                //do stuff
                return jObj;
            },
        _init: function(jObj, options) {
            
        }
        
	});
} (jQuery) );